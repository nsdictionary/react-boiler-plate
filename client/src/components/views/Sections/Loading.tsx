import React from "react";

const Loading = () => {
  return <div style={{
    width: '100%',
    height: '100vh',
    fontSize: '2rem',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  }}>Loading...</div>;
};

export default Loading;
